<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CPPLearner" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/light_grey</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/grey</item>
        <item name="colorSecondaryVariant">@color/dark_grey</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="actionBarStyle">@style/FlatActionBar</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CodeBlockBackground">
        <item name="android:background">@color/code_background</item>
        <item name="android:padding">8dp</item>
        <item name="android:textColor">@color/code_text</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <!-- ActionBar style -->
    <style name="FlatActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="elevation">0dp</item>
    </style>

</resources>